#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<int> ans;
    ans.insert(ans.end(), {1, 2, 5, 4, 5, 7, 8});
    for (int i : ans) {
        cout << i << " ";
    }
    cout << endl;

    return 0;
}